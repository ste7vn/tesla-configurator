import { VeichlePreviewPathPipe } from './veichle-preview-path.pipe';

describe('VeichlePreviewPathPipe', () => {
  it('create an instance', () => {
    const pipe = new VeichlePreviewPathPipe();
    expect(pipe).toBeTruthy();
  });
});
